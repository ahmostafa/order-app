export class User {
    title: string;
    firstName: string;
    lastName: string;
    email: string;
    dateOfBirth: string; // it can be also Date type

    // below data can be in address class
    street: string;
    postCode: number; // it can be string also
    homeNumber: string;
    placeOfResidence: string;
    country: string;
}

